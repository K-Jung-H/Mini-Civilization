# 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 모든 Codex agent와 thread의 장기 공통 규칙이다. 현재 작업 내용, TODO, 진행 상태는 기록하지 않는다.

## 작업 시작

1. `git status`를 확인한다.
2. 작업 범위와 관련된 uncommitted diff가 있으면 먼저 읽고, 다른 사용자 또는 thread의 변경을 보존한다.
3. 수정 전 관련 코드·Scene·Prefab·ScriptableObject를 최신 상태로 다시 확인한다.
4. 프로젝트 공통 규칙은 이 문서를 따른다. 필요한 경우에만 `Docs/PROJECT.md`, `Docs/ROADMAP.md`, `Docs/DECISIONS.md`의 관련 부분을 읽는다.

## 문서 선택과 정보 우선순위

- 새로운 시스템, 구조·데이터 모델·시스템 의존 관계 변경, 대규모 리팩터링에는 `Docs/PROJECT.md`의 관련 부분을 읽는다.
- milestone, 우선순위, 진행 상태가 필요한 작업에는 `Docs/ROADMAP.md`를 읽는다.
- 기존 구조의 이유를 확인하거나 아키텍처·저장 등 여러 시스템에 영향을 줄 변경을 제안할 때는 `Docs/DECISIONS.md`의 관련 결정을 읽는다.
- 단순하고 국소적인 수정에는 필요하지 않은 문서를 읽지 않는다.
- 정보가 충돌하면 현재 사용자의 명시적 지시, 실제 코드와 repository 상태, 이 문서, PROJECT.md, Active DECISIONS.md, ROADMAP.md, 과거 thread 순으로 우선한다.
- 문서와 코드가 중요하게 충돌하고 코드만으로 의도를 판단할 수 없으면 사용자에게 알린다.

## 조사와 수정

- 요청에 필요한 범위만 조사·수정한다.
- 기존 코드의 데이터 소유권, 변경 전파 경로, 저장 영향 범위를 확인한 뒤 수정한다.
- 증상만 가리는 임시 처방 대신 근본 원인을 찾는다. 장기 구조를 바꾸는 해결책이 필요하면 원인과 수정 제안을 설명하고 사용자 승인을 받는다.
- 다른 변경을 되돌리거나 덮어쓰지 않는다. 범위를 벗어난 변경이 작업을 막으면 사용자에게 알린다.

## Unity와 저장소

- Unity 에셋을 추가·이동·삭제할 때 대응 `.meta` 파일과 참조 무결성을 함께 확인한다.
- Scene, Prefab, ScriptableObject의 직렬화 참조를 깨지 않도록 한다.
- `Library/`, `Temp/`, `Logs/`, `UserSettings/`, 자동 생성 IDE 프로젝트 파일은 추적하지 않는다.
- 저장되는 월드 데이터 또는 생성 규칙을 변경할 때 저장 호환성, 생성 버전, 기존 저장 영향 여부를 확인한다.

## 검증

- agent는 자동 테스트, 회귀 검사, Unity Play Mode 실행, 성능 측정, 런타임 동작 검증을 실행하지 않는다.
- 코드 변경 후에는 컴파일 오류 확인까지만 수행한다. 컴파일 자체가 불필요하거나 실행할 수 없는 경우 그 사실을 명시한다.
- 작업 결과에는 사용자가 직접 확인할 테스트 항목, 재현 절차, 기대 결과를 변경 범위에 맞게 정리한다.
- 사용자가 전달한 테스트 결과는 후속 원인 분석과 수정의 근거로 사용한다.

## 장기 문서 관리

- 장기 컨텍스트는 repository의 현재 상태와 이 문서, `Docs/`를 source of truth로 삼는다. 과거 thread의 기억만으로 현재 구조를 판단하지 않는다.
- `Docs/PROJECT.md`는 현재 실제 구조만 설명한다.
- `Docs/ROADMAP.md`는 사용자에게 의미 있는 기능·milestone의 상태만 관리한다.
- `Docs/DECISIONS.md`는 확정된 중요 설계 결정과 그 변경 이력만 기록한다.
- 대화 전체, 아이디어, 검토 중인 대안, 실험, 임시 해결책, 사소한 구현 세부사항은 장기 문서에 옮기지 않는다.
- 작은 리팩터링·개별 파일 수정·실험·미확정 계획은 장기 문서에 기록하지 않는다.
- 주요 기능을 시작·완료·보류·취소하거나 milestone·우선순위가 바뀌면 `ROADMAP.md`를 갱신할 수 있다.
- 실제 주요 시스템 구조나 시스템 관계가 구현과 함께 바뀌면 `PROJECT.md`를 현재 구조로 갱신한다. 과거 구조는 남기지 않는다.
- 사용자가 중요한 설계를 명확히 선택하고 구현 방향으로 승인했을 때만 `DECISIONS.md`에 기록한다. 결정 여부가 불명확하면 기록하지 않고 확인한다.
- 기존 결정을 변경하면 삭제하지 않고 기존 결정은 `Superseded` 또는 `Reverted`로 갱신하고 대체 결정을 연결한다.
